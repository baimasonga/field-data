/*
Field photographs shown on the landing page.

Each entry names a file in `src/assets/images/landing/` and describes it. The
list is empty until real photographs are added, and the page simply leaves the
photo sections out while it is — better a page without photographs than one
carrying invented ones.

  file     the filename in src/assets/images/landing/
  alt      what is in the photograph, for anyone who cannot see it. Required.
  caption  the sector and place, shown under the image. Required.

Write both from the photograph in front of you. Do not describe work that did
not happen, and see the README in the images directory on permission before
publishing a photograph of someone.

Example, once you have the file:

  { file: 'water-point-bombali.jpg',
    alt: 'An enumerator holding a tablet beside a hand pump, talking with two
          residents drawing water.',
    caption: 'Water point survey, Bombali' }
*/

// eslint-disable-next-line import/prefer-default-export
export const landingPhotos = [];
