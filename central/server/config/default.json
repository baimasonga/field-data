{
  "default": {
    "database": {
      "host": "localhost",
      "user": "jubilant",
      "password": "jubilant",
      "database": "jubilant"
    },
    "server": {
      "port": 8383
    },
    "email": {
      "serviceAccount": "no-reply@getodk.org",
      "transport": "json"
    },
    "xlsform": {
      "host": "localhost",
      "port": 5001
    },
    "enketo": {
      "url": "http://localhost:8005/-",
      "apiKey": "enketorules"
    },
    "env": {
      "domain": "http://localhost:8989",
      "sysadminAccount": "no-reply@getodk.org"
    },
    "external": {
      "sentry": {},
      "analytics": {
        "_comment": "No endpoint: this deployment reports usage to nobody. Setting a url and formId here would switch central's analytics task back on, so leave them unset unless this project runs its own collector.",
        "version": "v2026.2.0_1"
      },
      "mailingListOptIn": {
        "_comment": "No endpoint: there is no mailing list, and a new user's email address is not sent anywhere. The sign-up checkbox is hidden to match; restore both together or neither.",
        "version": "v2025.4.0_1"
      },
      "s3blobStore": {}
    },
    "sessionLifetime": 86400
  },
  "test": {
    "database": {
      "host": "localhost",
      "user": "jubilant",
      "password": "jubilant",
      "database": "jubilant_test"
    },
    "email": {
      "serviceAccount": "no-reply@getodk.org",
      "transport": "json",
      "transportOpts": { "newline": "unix" }
    }
  }
}

